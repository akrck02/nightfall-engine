package constants

var (
	ScreenWidth  int = 42
	ScreenHeight int = 16
)

const (
	MAX_FPS           = 60.0 // Target frames per second
	MAX_UPS           = 60.0 // Target updates per second
	DEBUG_MODE        = true
	RENDER_LOCAL_FILE = false
)
